```mermaid
flowchart LR
    %% بخش کتاب‌ها
    subgraph بخش_کتابها["📚 بخش کتاب‌ها"]
        نوع_کتاب["انتخاب نوع کتاب"]
        موجودی["بررسی موجودی کتاب"]
    end

    %% بخش کاربر گیرنده
    subgraph گیرنده["👤 کاربر گیرنده"]
        ثبت_کاربر["ثبت‌نام/ورود کاربر"]
        درخواست_کتاب["ارسال درخواست کتاب"]
        دریافت_کتاب["دریافت کتاب"]
        بازگرداندن["بازگرداندن کتاب"]
    end

    %% بخش کاربر فرستنده
    subgraph فرستنده["👤 کاربر فرستنده"]
        ثبت_کتاب["ثبت کتاب برای امانت"]
        بررسی_درخواست["بررسی درخواست"]
        تایید["تایید یا رد درخواست"]
        تحویل["تحویل کتاب"]
        دریافت_بازگشت["دریافت کتاب بازگشتی"]
    end

    %% بخش فرآیند امانت
    subgraph فرآیند["🔄 فرآیند امانت"]
        ارسال_درخواست["ارسال درخواست"]
        تایید_امانت["ثبت تایید امانت"]
        ثبت_بازگشت["ثبت بازگشت کتاب"]
        امتیازدهی["امتیازدهی به طرف مقابل"]
    end

    %% ارتباط‌ها
    ثبت_کاربر --> درخواست_کتاب
    درخواست_کتاب --> ارسال_درخواست
    ارسال_درخواست --> بررسی_درخواست
    بررسی_درخواست --> تایید
    تایید --> تحویل
    تحویل --> دریافت_کتاب
    دریافت_کتاب --> بازگرداندن
    بازگرداندن --> ثبت_بازگشت
    ثبت_بازگشت --> دریافت_بازگشت
    ثبت_بازگشت --> امتیازدهی

    نوع_کتاب --> موجودی
    موجودی --> درخواست_کتاب
    ثبت_کتاب --> بررسی_درخواست
